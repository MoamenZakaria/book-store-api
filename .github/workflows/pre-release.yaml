name: Create release

on:
  workflow_dispatch:
    inputs:
      is_prerelease:
        description: 'Mark as Pre-Release? (true/false)'
        required: true
        default: 'true'
  push:
    tags:
      - v*

permissions:
  contents: write

jobs:
  release:
    name: Release pushed tag
    runs-on: ubuntu-22.04
    steps:
      - name: Check if release already exists
        id: check_release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}
        run: |
          echo "RELEASE_EXISTS=false" >> $GITHUB_ENV
          if gh release view "$tag" --repo="$GITHUB_REPOSITORY"; then
            echo "RELEASE_EXISTS=true" >> $GITHUB_ENV
          fi

      - name: Create or Update release
        if: env.RELEASE_EXISTS == 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}
        run: |
          gh release create "$tag" \
              --repo="$GITHUB_REPOSITORY" \
              --title="${GITHUB_REPOSITORY#*/} ${tag#v}" \
              --generate-notes \
              ${{ github.event.inputs.is_prerelease == 'true' && '--prerelease' || '' }}

      - name: Update existing release
        if: env.RELEASE_EXISTS == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}
        run: |
          echo "Release for tag $tag already exists. Updating release..."
          gh release edit "$tag" \
              --repo="$GITHUB_REPOSITORY" \
              --title="${GITHUB_REPOSITORY#*/} ${tag#v}" \
              --prerelease ${{ github.event.inputs.is_prerelease == 'true' && 'true' || 'false' }}

